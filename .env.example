# ต้นทางราคาสด (ไม่ตั้งค่า = ใช้ GraphQL ของ craft-world.gg ตาม DEFAULT_ENDPOINT ใน lib/feed.ts)
#
# ต้องเป็น GraphQL ที่รับ query exactInputQuote(input: ExactInputInput!) แล้วคืน output { symbol amount }
# ตัวดึงยิงสองทิศต่อ symbol: ขาย = SYM -> COIN จำนวน 1 · ซื้อ = COIN -> SYM ด้วยมูลค่าราวหนึ่งชิ้น
# ถ้าต้นทางใช้รูปแบบอื่น แก้ที่ lib/feed.ts (การยิง) และ parsePriceFeed() ใน lib/prices.ts (การแปลง)
#
# ดึงไม่สำเร็จหรือราคาไม่ครบทุกไอเทมที่สูตรใช้ = ตกกลับไปใช้ snapshot ใน data/prices.json
# แล้วหน้าเว็บจะขึ้นป้าย snapshot พร้อมเหตุผลให้เห็น
PRICE_API_URL=
