# ต้นทางราคาสด — ตั้งค่านี้แล้วหน้าเว็บจะดึงราคาใหม่ทุก 60 วินาที
#
# ต้องตอบ JSON รูปแบบ { "buy": { "SYMBOL": number, ... }, "sell": { "SYMBOL": number, ... } }
# โดยมีราคาครบทุกไอเทมที่สูตรใช้ ไม่งั้นจะถูกปฏิเสธและตกกลับไปใช้ snapshot
# ถ้าต้นทางใช้รูปแบบอื่น แก้ที่ parsePriceFeed() ใน lib/prices.ts จุดเดียว
#
# ไม่ตั้งค่า = ใช้ราคา snapshot ใน data/prices.json (หน้าเว็บจะขึ้นป้าย snapshot ให้เห็นชัด)
PRICE_API_URL=
